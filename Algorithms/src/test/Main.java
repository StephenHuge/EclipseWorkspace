package test;

import java.util.*;

/*public class Main {
    public void buyHat() {
        Scanner num = new Scanner(System.in);
        int num1 = num.nextInt();
        int[] arr = new int[num1];
        int pivot = 0;
		Scanner sc2 = new Scanner(System.in);
        while(sc2.hasNext()){
            int m = sc2.nextInt();
			arr[pivot] = m;
            pivot++;
        }
        arr = sort(arr);
        if(arr[0] == arr[num1 - 1]) {
            System.out.println(-1);
        }
        int a0 = arr[0];
        pivot = 1;
        while((pivot <= num1 - 1) && arr[pivot] <= arr[0]) {
            pivot++;
        }
        int a1 = arr[pivot];
        while((pivot <= num1 - 1) && arr[pivot] <= arr[a1]) {
            pivot++;
        }
        int a2 = arr[pivot];
        System.out.println(a2);
    }
    
    public int[] sort(int[] arr) {
        int temp;
        for(int i = 0; i < arr.length - 1; i++) {
            for (int j = 1; j > i; j++) {
                if(arr[i] < arr [j]) {
                    temp = arr[j];
                    arr[j] = arr[i];
                    arr[i] = temp;
                }
            }
        }
        return arr;
    }
    public static void main(String[] args) {
        Main bh = new Main();
        bh.buyHat();
    }
}
*/
import java.util.*;

//所有的输入和输出练习
public class Main {
/*	 public static void main(String[] args) {
	     
	      * 循环连续输入和输出
	      * 输入：一个数字 3
	      *     一个数组 1 2 3
	      
		Scanner x = new Scanner(System.in);
		while(x.hasNext()){
			int m = x.nextInt();
			int[] number = new int[m];
			for(int i = 0;i < m;i++) {
				number[i] = x.nextInt();
			}
			number = sort(number);
		}
		
	}*/

}